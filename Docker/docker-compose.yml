version: '3.8'

services:
  webchat:
    build: .
    container_name: webchat
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    volumes:
      - ./data:/app/data
    restart: unless-stopped